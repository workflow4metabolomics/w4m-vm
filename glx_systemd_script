# Galaxy systemd service script

[Unit]
Description=Galaxy service
#After=network.target

[Service]
Type=oneshot
RemainAfterExit=true
PIDFile=/home/vagrant/galaxy/galaxy.pid
ExecStart=/home/vagrant/galaxy/run.sh --daemon
ExecStop=/home/vagrant/galaxy/run.sh --stop-daemon
Restart=no # Must be "no" for "oneshot" type.
User=vagrant
Group=vagrant

[Install]
WantedBy=multi-user.target
